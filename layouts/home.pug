//- home.pug - Home page layout.
extends layout.pug

include mixin.pug

block content
  for entry in pagination.files
    +entrylink(entry.path,entry.title,entry.date)
  
  +paginate

  script(type='text/javascript').
    type: 'urgent' 
    always_show_widget: true
